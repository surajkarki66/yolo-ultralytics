# Detection models

Place the **quantized model produced by Vitis AI** here (e.g. `*_int.xmodel` or the deployment artifact used by the Detect evaluation scripts).

The evaluation pipeline in this directory (`evaluate.py`, `post_processing.py`) expects the model format and I/O used by the Vitis AI compilation output for YOLOv8 detection.
