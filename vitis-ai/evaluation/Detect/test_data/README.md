# Detection test data

Place the **test set** used for evaluating the quantized/compiled detection model here. This typically includes images and corresponding labels in the format expected by `evaluate.py` and `coco_prep.py` (e.g. COCO or YOLO-style annotations). See the parent `evaluation/README.md` and `coco_prep.py` for the exact layout.
