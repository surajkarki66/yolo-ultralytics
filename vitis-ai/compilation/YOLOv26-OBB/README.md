# YOLOv26-OBB model

Place the quantized **OBB** (oriented bounding box) XIR model here (e.g. `OBBModel_int.xmodel`) before running `run_compile.sh` or `vai_c_xir` from the parent `compilation/` directory. See `../README.md` for the compilation procedure.
