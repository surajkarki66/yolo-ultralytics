# YOLOv8 detection model

Place the quantized **detection** XIR model here (e.g. `DetectionModel_int.xmodel`) before running `run_compile.sh` or `vai_c_xir` from the parent `compilation/` directory. See `../README.md` for the compilation procedure.
